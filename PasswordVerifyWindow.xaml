<Window x:Class="EyeGuard.PasswordVerifyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="验证密码" Height="260" Width="380"
        WindowStartupLocation="CenterScreen"
        FocusManager.FocusedElement="{Binding ElementName=PasswordBox}"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Background="{StaticResource SurfaceBrush}">
    <Grid Margin="24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="🔒  密码验证" Style="{StaticResource SectionTitleText}" Grid.Row="0"/>
        <TextBlock Style="{StaticResource CaptionText}" Margin="0,0,0,12"
                   Text="请输入密码以继续操作" Grid.Row="1"/>

        <PasswordBox x:Name="PasswordBox" Style="{StaticResource StyledPasswordBox}"
                     Margin="0,0,0,8" KeyDown="PasswordBox_KeyDown" Grid.Row="2"/>

        <TextBlock x:Name="ErrorText" Style="{StaticResource ErrorText}"
                   Margin="0,0,0,12" Visibility="Collapsed" Grid.Row="4"/>

        <Button x:Name="VerifyButton" Content="验证" Style="{StaticResource PrimaryButton}"
                Click="VerifyButton_Click" Grid.Row="5"/>
    </Grid>
</Window>